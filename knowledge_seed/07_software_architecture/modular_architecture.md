# Modular Architecture
A modular architecture separates concerns into components with explicit responsibilities. Typical layers include user interfaces, API routes, services, data access, knowledge pipelines and deployment configuration.
