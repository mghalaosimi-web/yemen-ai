# Yemen AI v6.4 — Stability & Login Reliability Report

## Improvements
- Updated runtime version to 6.4.0.
- Added development-only repair for documented demo credentials when an older local SQLite database contains stale prototype hashes.
- Production mode preserves existing account credentials.
- Added favicon endpoint to remove the harmless but noisy `/favicon.ico` 404 during browser startup.
- Kept all existing datasets, knowledge, conversations and architecture intact.

## Validation target
- Existing automated suite must remain green.
- Demo accounts: admin, developer, trainer, user.
- Documented demo password: `YemenAI2026!`.

## Security note
The automatic credential repair is active only when `YEMEN_AI_ENV=development`. In production, existing user passwords are never overwritten by startup logic.
