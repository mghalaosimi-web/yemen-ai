# Yemen AI v8.6 — Adaptive Cognitive Architecture

## الهدف
نقل Yemen AI من استجابة قائمة على السؤال فقط إلى دورة معرفية منظمة:

**سياق → حل الإشارة → تخطيط → استرجاع → اختيار ذاكرة مرتبطة → استدلال → إجابة → حفظ انتقائي**

## ما أضيف
- محرك تخطيط معرفي محلي `CognitiveOrchestrator`.
- حل أسئلة المتابعة القصيرة مثل «اشرح أكثر» بالرجوع إلى آخر موضوع للمستخدم.
- تصنيف أسلوب المهمة إلى: خطة، مقارنة، تلخيص، سبب، شرح عميق، إجابة مباشرة.
- أهداف فرعية داخلية لكل نوع مهمة.
- اختيار الذاكرة المرتبطة بالسؤال بدل تمرير كل الذاكرة.
- كشف تحفظي لإشارات تضارب الأدلة.
- حفظ انتقائي للحقائق التي يصرح بها المستخدم، بدل تخزين كل رسالة كذاكرة دائمة.
- إظهار معلومات الخطة ضمن metadata/debug API فقط، وليس داخل نص المستخدم.

## حدود مهمة
هذه البنية لا تدّعي تدريب نموذج لغوي بحجم ChatGPT. إنها طبقة استدلال وتخطيط وذاكرة فوق المعرفة المحلية، وتتحسن بزيادة المعرفة والتقييمات والتصحيحات الموثوقة.
